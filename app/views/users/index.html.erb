<% provide(:title, "ゆーざー") %>

<h2>ゆーざー</h2>

<div class="row">
  <ul class="users">
    <% @users.each do |user| %>
      <div class="col-md-4">
        <%= render user %>
      </div>
    <% end %>
  </ul>
</div>

<%= will_paginate %>