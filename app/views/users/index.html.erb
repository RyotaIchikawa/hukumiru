<% provide(:title, "ゆーざー") %>

<h2>ゆーざー</h2>

<div class="row">
  <ul class="users">
    <% @users.each do |user| %>
      <div class="col-md-3 col-xs-6 thumbnail">
        <%= render user %>
      </div>
    <% end %>
  </ul>
</div>

<%= will_paginate %>