<% provide(:title, "ゆーざー") %>

<div class="container">
  <h2>ゆーざー</h2>

  <div class="row">
    <ol class="users">
      <% @users.each do |user| %>
        <div class="col-xs-12 col-md-3">
          <%= render user %>
        </div>
      <% end %>
    </ol>
  </div>

  <%= paginate @users %>
</div>