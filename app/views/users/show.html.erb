<% provide(:title, current_user.username + "のぷろふぃーる") %>

<div class="container">
  <div class="row">
    <section class="col-md-3">
      <%= image_tag current_user.image, class: "img-circle  thumbnail" %>
      <h2><%= @user.username %></h2>
    </section>
  </div>
</div>

<div class="container">
  <% if @user.microposts.any? %>
    <h2>ふく (<%= @user.microposts.count %>まい)</h2>
    <div class="row">  
      <ol class="microposts">
        <% @microposts.each do |micropost| %>
        <div class="col-md-4 col-xs-3">
          <%= render micropost %>
        </div>
        <% end %>
      </ol>
      <%= will_paginate @microposts %>
    </div>      
  <% end %>
</div>